## 1. Model danych i mocki

- [x] 1.1 Rozszerzenie interfejsu `Venue` w `src/types/index.ts` o pola `highlights`, `bestFor` i `atmosphere`.
- [x] 1.2 Aktualizacja `INITIAL_VENUES` w `src/data/mockVenues.ts` o nowe dane dla każdego lokalu.

## 2. Widok wyszukiwania

- [x] 2.1 Implementacja wyświetlania wyróżników (highlights) pod nazwą lokalu na karcie wyników w `MobileSearchView.tsx`.

## 3. Widok szczegółów lokalu

- [x] 3.1 Implementacja sekcji "Dlaczego ten lokal?" z ikonami USP w `MobileVenueDetailModal.tsx`.
- [x] 3.2 Dodanie informacji o przeznaczeniu (`bestFor`) i atmosferze lokalu w layoutcie `MobileVenueDetailModal.tsx`.

## 4. Weryfikacja

- [x] 4.1 Uruchomienie `npm run build`, aby upewnić się, że zmiany są zgodne z systemem typów.
